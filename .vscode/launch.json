{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Project",
            "type": "python",
            "request": "launch",
            "program": "${workspaceFolder}/main.py",
            "cwd": "${workspaceFolder}",
        }
    ]
}